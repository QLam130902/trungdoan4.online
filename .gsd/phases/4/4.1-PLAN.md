---
phase: 4
plan: 1
wave: 1
---

# Plan 4.1: Tăng thời gian sống của JWT Token & Kiểm tra phân quyền API

## Objective
Tăng thời gian sống của token JWT ở Backend từ 5 phút lên 24 giờ (hỗ trợ cấu hình qua file properties), đồng thời rà soát cấu hình Spring Security cùng việc xử lý mã lỗi 401 để đảm bảo các vai trò cán bộ (Officer/Admin) không bị đẩy phiên đăng nhập không mong muốn khi thực hiện tác vụ thông thường.

## Context
- .gsd/SPEC.md
- .gsd/ARCHITECTURE.md
- [JwtService.java](file:///c:/Users/PC/projects/trungdoan4.server/src/main/java/vn/homthugopy/config/JwtService.java)
- [SecurityConfig.java](file:///c:/Users/PC/projects/trungdoan4.server/src/main/java/vn/homthugopy/config/SecurityConfig.java)
- [AuthContext.jsx](file:///c:/Users/PC/projects/trungdoan4.online/src/contexts/AuthContext.jsx)

## Tasks

<task type="auto">
  <name>Backend: Tối ưu hóa thời gian sống của JWT Token</name>
  <files>
    - c:\Users\PC\projects\trungdoan4.server\src\main\java\vn\homthugopy\config\JwtService.java
  </files>
  <action>
    - Cập nhật biến `EXPIRATION_TIME` trong `JwtService.java` để hỗ trợ cấu hình động qua annotation `@Value("${jwt.expiration:86400000}")` (mặc định 24 giờ).
    - Điều chỉnh code sinh token sử dụng giá trị cấu hình này thay vì giá trị cố định 300000 (5 phút).
  </action>
  <verify>
    Chạy biên dịch và khởi động backend, kiểm tra thời gian hết hạn (`exp`) của token khi đăng nhập để đảm bảo kéo dài đúng 24 giờ.
  </verify>
  <done>
    Thời gian hết hạn của JWT Token được nâng lên 24 giờ và có thể cấu hình linh hoạt.
  </done>
</task>

<task type="auto">
  <name>Kiểm tra phân quyền bảo mật API (Spring Security)</name>
  <files>
    - c:\Users\PC\projects\trungdoan4.server\src\main\java\vn\homthugopy\config\SecurityConfig.java
  </files>
  <action>
    Rà soát tệp `SecurityConfig.java` để đảm bảo các endpoint:
    - `/suggestions/**` có thể truy cập bởi cả cán bộ (`ROLE_OFFICER`) và quản trị viên (`ROLE_ADMIN`).
    - Các API thống kê và phản hồi không chặn vai trò `ROLE_OFFICER` dẫn đến lỗi 401 giả lập lỗi hết hạn phiên.
  </action>
  <verify>
    Biên dịch lại backend và kiểm tra quyền truy cập API bằng các tài khoản cán bộ có vai trò khác nhau (Officer / Admin).
  </verify>
  <done>
    Không có API thông thường nào trả về mã lỗi 401 khi cán bộ xử lý thao tác đúng quyền hạn.
  </done>
</task>

<task type="auto">
  <name>Frontend: Rà soát cơ chế bắt lỗi session trong AuthContext</name>
  <files>
    - c:\Users\PC\projects\trungdoan4.online\src\contexts\AuthContext.jsx
  </files>
  <action>
    Kiểm tra logic xử lý HTTP Status 401 trong hàm `authFetch` của `AuthContext.jsx`. Đảm bảo cơ chế hiển thị thông báo hết hạn phiên chỉ kích hoạt khi token thực sự hết hạn hoặc bị từ chối xác thực bởi backend, tránh reload trang gây mất token.
  </action>
  <verify>
    Chạy dev server Frontend, thực hiện đăng nhập và chuyển hướng giữa các trang Admin (Góp ý, Thống kê, Cán bộ) mà không bị log out đột ngột.
  </verify>
  <done>
    Giao diện quản lý chạy ổn định và giữ phiên đăng nhập hoạt động trơn tru.
  </done>
</task>

## Success Criteria
- [ ] JWT Token của cán bộ sau khi đăng nhập có thời gian sống ít nhất là 24 giờ.
- [ ] Cán bộ cấp dưới (Officer) truy cập danh sách góp ý và thực hiện phản hồi không gặp lỗi 401.
- [ ] Tình trạng tự động đăng xuất ngay khi tải trang quản trị được khắc phục hoàn toàn.
