---
phase: 5
plan: 1
wave: 1
---

# Plan 5.1: Đồng bộ hóa trạng thái hết hạn phiên và tái cấu trúc Provider

## Objective
Khắc phục triệt để lỗi kẹt modal "Phiên đăng nhập hết hạn" bằng cách chuyển `<HashRouter>` ra ngoài `<AuthProvider>` để `AuthContext` có thể sử dụng hook `useLocation` theo dõi sự thay đổi của route, từ đó ẩn/hiện và reset modal một cách chính xác.

## Context
- .gsd/SPEC.md
- [App.jsx](file:///c:/Users/PC/projects/trungdoan4.online/src/App.jsx)
- [AuthContext.jsx](file:///c:/Users/PC/projects/trungdoan4.online/src/contexts/AuthContext.jsx)
- [ProtectedRoute.jsx](file:///c:/Users/PC/projects/trungdoan4.online/src/components/ProtectedRoute.jsx)

## Tasks

<task type="auto">
  <name>Tái cấu trúc Provider trong App.jsx</name>
  <files>
    - c:/Users/PC/projects/trungdoan4.online/src/App.jsx
  </files>
  <action>
    Đưa `<HashRouter>` ra ngoài `<AuthProvider>` trong `App.jsx` để các hook của React Router (như `useLocation`) có thể được sử dụng bên trong `AuthProvider`.
  </action>
  <verify>
    Chạy build thử hoặc kiểm tra xem frontend có gặp lỗi khởi tạo Router không.
  </verify>
  <done>
    Cấu trúc trong App.jsx chuyển thành:
    &lt;HashRouter&gt;
      &lt;AuthProvider&gt;
        &lt;Routes&gt;
          ...
        &lt;/Routes&gt;
      &lt;/AuthProvider&gt;
    &lt;/HashRouter&gt;
  </done>
</task>

<task type="auto">
  <name>Cập nhật AuthContext để theo dõi route thông qua useLocation</name>
  <files>
    - c:/Users/PC/projects/trungdoan4.online/src/contexts/AuthContext.jsx
  </files>
  <action>
    - Import `useLocation` từ `react-router-dom`.
    - Sử dụng `useLocation()` để lấy location hiện tại của ứng dụng.
    - Dùng một `useEffect` theo dõi `location.pathname`. Nếu route chuyển về `/admin/login` hoặc các route không phải `/admin` (ví dụ trang client), tự động set `isSessionExpired(false)`.
    - Cập nhật điều kiện hiển thị modal "Phiên đăng nhập hết hạn" để chỉ hiển thị khi `isSessionExpired === true` VÀ route hiện tại bắt đầu bằng `/admin` nhưng KHÔNG PHẢI `/admin/login`.
    - Loại bỏ sự kiện lắng nghe `hashchange` cũ vì đã có `useLocation` quản lý đồng bộ.
  </action>
  <verify>
    Kiểm tra xem file compile thành công mà không có lỗi cú pháp hay thiếu import.
  </verify>
  <done>
    - AuthContext.jsx sử dụng useLocation() thành công.
    - Modal hết hạn không bao giờ hiển thị khi pathname là `/admin/login` hoặc trang client.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Kiểm thử và xác nhận lỗi đã sửa triệt để</name>
  <files>
    - c:/Users/PC/projects/trungdoan4.online/src/contexts/AuthContext.jsx
  </files>
  <action>
    - Yêu cầu người dùng F5 tải lại trang.
    - Thử đăng nhập vào trang quản trị cán bộ.
    - Sau khi đăng nhập, giả lập token hết hạn hoặc restart server backend, rồi thao tác click vào một tính năng (ví dụ: chuyển trang góp ý) để kích hoạt API call 401.
    - Xác nhận hệ thống tự động redirect về trang login mà không còn hiện tượng kẹt modal hết hạn đè lên giao diện đăng nhập nữa.
  </action>
  <verify>
    Xác nhận trực quan trên màn hình trình duyệt:
    - Khi bị redirect về login do hết hạn token, modal không bị kẹt.
    - Giao diện đăng nhập hiển thị sạch sẽ để cán bộ nhập lại tài khoản mật khẩu.
  </verify>
  <done>
    Người dùng xác nhận việc khắc phục lỗi hết hạn token thành công.
  </done>
</task>

## Success Criteria
- [ ] Ứng dụng React hoạt động bình thường, không bị lỗi Router context.
- [ ] Khi API trả về 401, hệ thống tự động redirect về trang `/admin/login` và ẩn modal hết hạn phiên đăng nhập lập tức, không che khuất form đăng nhập.
- [ ] Không có hiện tượng kẹt modal khi reload lại trang đăng nhập.
