---
phase: 3
plan: 2
wave: 1
---

# Plan 3.2: API Phân quyền theo Đơn vị & Quản lý Tài khoản (Backend)

## Objective
Xây dựng logic phân quyền xem góp ý dựa trên cơ cấu phân cấp đơn vị, tích hợp mã đơn vị (`unitCode`) vào token JWT của cán bộ khi đăng nhập, cập nhật API lấy danh sách góp ý, và nâng cấp API quản lý tài khoản để gán đơn vị cho cán bộ.

## Context
- .gsd/SPEC.md
- .gsd/ARCHITECTURE.md
- [SuggestionService.java](file:///c:/Users/PC/projects/trungdoan4.server/src/main/java/vn/homthugopy/suggestion/service/SuggestionService.java)
- [SuggestionController.java](file:///c:/Users/PC/projects/trungdoan4.server/src/main/java/vn/homthugopy/suggestion/controller/SuggestionController.java)
- [UserController.java](file:///c:/Users/PC/projects/trungdoan4.server/src/main/java/vn/homthugopy/user/controller/UserController.java)
- [JwtService.java](file:///c:/Users/PC/projects/trungdoan4.server/src/main/java/vn/homthugopy/config/JwtService.java)

## Tasks

<task type="auto">
  <name>Bổ sung unitCode vào JWT Claim</name>
  <files>
    - c:\Users\PC\projects\trungdoan4.server\src\main\java\vn\homthugopy\config\JwtService.java
  </files>
  <action>
    Cập nhật hàm sinh token trong `JwtService.java` để chèn thêm claim `unitCode` từ thực thể `User`.
    Điều này giúp client (React SPA) có thể parse token và lấy ngay `unitCode` của cán bộ mà không cần gọi thêm API lấy thông tin cá nhân.
  </action>
  <verify>
    Chạy thử đăng nhập bằng Postman hoặc curl, kiểm tra cấu trúc JWT (qua jwt.io) để chắc chắn claim `unitCode` đã hiện diện.
  </verify>
  <done>
    JWT Token sinh ra chứa claim `unitCode` chính xác của tài khoản đăng nhập.
  </done>
</task>

<task type="auto">
  <name>Cập nhật Logic Phân quyền xem Góp ý (Data Isolation)</name>
  <files>
    - c:\Users\PC\projects\trungdoan4.server\src\main\java\vn\homthugopy\suggestion\service\SuggestionService.java
  </files>
  <action>
    Cập nhật các hàm truy vấn danh sách góp ý trong `SuggestionService.java`:
    - Nếu cán bộ có `role` là `ROLE_ADMIN` (Trung đoàn): Xem được tất cả góp ý.
    - Nếu cán bộ thuộc cấp Tiểu đoàn (ví dụ: `TD1`): Lấy ra danh sách các `unitCode` con (ví dụ: `c1`, `c2`, `c3`, `c4`, `KTT_TD1` và chính `TD1`), sau đó truy vấn các góp ý thuộc nhóm đơn vị này.
    - Nếu cán bộ thuộc cấp Đại đội hoặc Khối trực thuộc (ví dụ: `c1`, `c14`): Chỉ truy vấn các góp ý có `unitCode` bằng chính đơn vị đó.
  </action>
  <verify>
    Viết test case tích hợp hoặc chạy thử bằng các tài khoản cán bộ cấp Tiểu đoàn và Đại đội khác nhau để đối chiếu dữ liệu trả về.
  </verify>
  <done>
    Dữ liệu danh sách góp ý được lọc chính xác theo phạm vi phân cấp quản lý của cán bộ đăng nhập.
  </done>
</task>

<task type="auto">
  <name>Nâng cấp API Quản lý Tài khoản Cán bộ</name>
  <files>
    - c:\Users\PC\projects\trungdoan4.server\src\main\java\vn\homthugopy\user\controller\UserController.java
    - c:\Users\PC\projects\trungdoan4.server\src\main\java\vn\homthugopy\user\dto\UserRequestDTO.java
  </files>
  <action>
    Cập nhật API tạo mới cán bộ và cập nhật cán bộ trong `UserController.java`:
    - Nhận trường `unitCode` trong `UserRequestDTO`.
    - Kiểm tra xem `unitCode` đó có tồn tại trong bảng `units` hay không trước khi lưu.
    - Gán `unitCode` vào thực thể `User` trước khi lưu vào Database.
  </action>
  <verify>
    Chạy thử API POST `/users` với `unitCode` hợp lệ và kiểm tra trong database trường `unit_code` của user mới được tạo.
  </verify>
  <done>
    Admin cấp Trung đoàn có thể tạo tài khoản và chỉ định đơn vị trực thuộc cho cán bộ cấp dưới thông qua API.
  </done>
</task>

## Success Criteria
- [ ] Token đăng nhập của cán bộ chứa thông tin `unitCode`.
- [ ] Cán bộ đăng nhập cấp Tiểu đoàn chỉ thấy góp ý của các đại đội trực thuộc và khối trực thuộc của tiểu đoàn đó.
- [ ] Cán bộ Đại đội chỉ thấy góp ý thuộc đơn vị mình.
- [ ] API tạo tài khoản cán bộ lưu thành công mã đơn vị (`unitCode`).
